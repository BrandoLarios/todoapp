<div class="small-container center-block">
  
    <div class="padding">
      <h3> <%= @list.name %> </h3>
    </div>
    <div class="padding">
      <p> <%=  @list.description %> </p>
    </div>
    <div class="padding">
      <%= link_to "Editar", edit_list_path(@list), class:"btn btn-warning" %> 
      <%= link_to "Eliminar", @list, method: :delete, class:"btn btn-danger" %> 
    </div>
</div>